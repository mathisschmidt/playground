create a chrome extansion for show this easy_show_data.

Idea of the tool create view (see what's done in easy_data).
A view can be only print data or only a graph based on teh data or both. 
The spirit is to stay minimalist and easy to use. The view have to be easy readable on a Chrome extension.
The challenge: just pure JS + light library (no heavy framework like react/vue/angular).

TODO:
- [ ] Create a basic table view
  - [ ] add/delete/update data
  - [ ] add/delete/update column
- [ ] create a DB services for manage data (many abstraction for can change the DB provider in future)


Maybe:
- [ ] jsdoc + check types