[project]
name = "python-class-viewer"
version = "0.1.0"
description = "Web application that generates class diagrams from Python files"
requires-python = ">=3.13"
dependencies = [
    "nicegui>=1.3.8"
]
